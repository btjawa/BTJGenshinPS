import{_ as t}from"./plugin-vue_export-helper-c27b6911.js";import{r as a,o as d,c as r,e as n,a as o,b as e,d as p,f as i}from"./app-2527d5d5.js";const s={},h=i('<h2 id="自动化运行服务" tabindex="-1"><a class="header-anchor" href="#自动化运行服务" aria-hidden="true">#</a> 自动化运行服务</h2><h3 id="本地端" tabindex="-1"><a class="header-anchor" href="#本地端" aria-hidden="true">#</a> 本地端</h3><p>请确保在 <code>主页</code> 选项卡下将 <strong>Grasscutter</strong> 的参数填写为</p><p><code>IP/域名 127.0.0.1</code> <code>游戏服务器端口 22102</code> <code>DISPATCH端口 443</code></p><p><code>客户端代理</code> 一栏的端口应该对应Grasscutter中的DISPATCH端口</p><p>点击右下角的 <code>启动全部服务</code> 来启动服务，点击 <code>选择游戏路径</code> 添加路径，点击 <code>启动游戏</code> 启动游戏</p><p>等待Grasscutter加载完毕后使用 <code>/account create 用户名</code> 创建账户</p><p>并在游戏内使用你的用户名（没有设置密码，可以输入≥1位的字符当作密码，例如&quot;1&quot;）来登录</p><h3 id="远程端" tabindex="-1"><a class="header-anchor" href="#远程端" aria-hidden="true">#</a> 远程端</h3><h4 id="服务器配置" tabindex="-1"><a class="header-anchor" href="#服务器配置" aria-hidden="true">#</a> 服务器配置</h4><p>请确保在 <code>主页</code> 选项卡下将 <strong>Grasscutter</strong> 的参数按如下指引填写</p><p><code>IP/域名</code> 你服务器的内网/公网IP，若是虚拟机或是内网环境请填写内网IP</p><div class="hint-container tip"><p class="hint-container-title">提示</p><p>Windows下ip可在cmd中使用 <code>ipconfig</code> 查看，找到对应网卡下的IPv4或IPv6地址</p><p>Linux各发行版下可使用 <code>ifconfig</code> 查看，找到对应网卡下的inet或inet6</p><p>此处RedHat/CentOS可使用 <code>yum -y install net-tools</code> ，Debian/Ubuntu可使用 <code>apt install net-tools</code></p><h4 id="内网ip" tabindex="-1"><a class="header-anchor" href="#内网ip" aria-hidden="true">#</a> 内网IP</h4><p>分为A类、B类、C类</p><p>A类地址：10.0.0.0 ~ 10.255.255.255</p><p>B类地址：172.16.0.0 ~ 172.31.255.255</p><p>C类地址：192.168.0.0 ~ 192.168.255.255</p><p>请确保你找到的ip不是127.0.0.1或localhost</p><p>并在<strong>本地</strong>可以ping到，延迟&gt;1ms，在服务器可以ping到，延迟&lt;1ms</p><h4 id="公网ip" tabindex="-1"><a class="header-anchor" href="#公网ip" aria-hidden="true">#</a> 公网IP</h4><p>此处只需要确保找到的不是内网ip，或127.0.0.1/localhost</p><p>并在<strong>本地</strong>可以ping到，延迟&gt;1ms，在服务器可以ping到，延迟&lt;1ms</p></div><p><code>游戏服务器端口</code> 应是gc目录下 <code>config.json</code> ，<code>server.game.bindPort</code> 的键值</p><p><code>DISPATCH端口</code> 应是gc目录下 <code>config.json</code> ，<code>server.http.bindPort</code> 的键值</p><p>在服务端点击 <code>启动全部服务</code> 启动后，可安全关闭代理</p><p>随后在防火墙放出 UDP <code>22102</code>，TCP <code>443</code> 或你自己选择的Dispatch端口</p><p>若是腾讯云、阿里云等需要在安全组额外放出</p><h4 id="本地配置" tabindex="-1"><a class="header-anchor" href="#本地配置" aria-hidden="true">#</a> 本地配置</h4><p>在 <code>&quot;客户端代理&quot;</code> 配置好服务器的公网IP与Dispatch端口，点击 <code>启动代理</code> 来启动代理</p><p>点击 <code>选择游戏路径</code> 添加路径，点击 <code>启动游戏</code> 启动游戏</p><p>等待Grasscutter加载完毕后使用 <code>/account create 用户名</code> 创建账户</p><p>并在游戏内使用你的用户名（没有设置密码，可以输入≥1位的字符当作密码，例如&quot;1&quot;）来登录</p><h2 id="手动运行服务" tabindex="-1"><a class="header-anchor" href="#手动运行服务" aria-hidden="true">#</a> 手动运行服务</h2><p>咕咕咕...</p>',25),l={href:"https://github.com/Grasscutters/Grasscutter/wiki",target:"_blank",rel:"noopener noreferrer"};function u(g,f){const c=a("ExternalLinkIcon");return d(),r("div",null,[n(" more "),h,o("p",null,[e("可以去 "),o("a",l,[e("Grasscutter Wiki"),p(c)]),e(" 查看")])])}const P=t(s,[["render",u],["__file","running.html.vue"]]);export{P as default};
